mutation Login($username: String!, $password: String!) {
  login(input: { identifier: $username, password: $password }) {
    user {
      email
    }
    jwt
  }
}

query Me {
  me {
    id
    username
    email
    confirmed
    blocked
    role {
      name
    }
  }
}
