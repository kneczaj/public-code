import { makeStyles } from '@material-ui/core/styles';

/**
 * Styles containing classes to fill the parent size
 */
export const useStylesFill = makeStyles(() => ({
  fill: {
    height: '100%',
    width: '100%'
  }
}));
